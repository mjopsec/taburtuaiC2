#pragma once
/*
 * config.h — generated by taburtuai-generate for each implant build.
 * DO NOT commit with real values. This template uses @@PLACEHOLDER@@ tokens
 * replaced by cmd/generate/generator_c.go at build time.
 */

/* C2 server URL — e.g. "https://c2.example.com" */
#define CFG_SERVER_URL       "@@SERVER_URL@@"

/* AES encryption key (plaintext; SHA-256 hashed to derive 256-bit key) */
#define CFG_ENC_KEY          "@@ENC_KEY@@"

/* Secondary key (currently unused in C implant; reserved) */
#define CFG_SEC_KEY          "@@SEC_KEY@@"

/* Per-binary random salt — ensures distinct UUIDs on the same host */
#define CFG_INSTANCE_SALT    "@@INSTANCE_SALT@@"

/* Beacon timing */
#define CFG_INTERVAL_SEC     @@INTERVAL@@
#define CFG_JITTER_PCT       @@JITTER@@
#define CFG_MAX_RETRIES      @@MAX_RETRIES@@

/* Kill date — empty string means never */
#define CFG_KILL_DATE        "@@KILL_DATE@@"

/* Working hours guard (0 = disabled) */
#define CFG_WORK_HOURS_ONLY  @@WORK_HOURS_ONLY@@
#define CFG_WORK_START       @@WORK_START@@
#define CFG_WORK_END         @@WORK_END@@

/* OPSEC feature flags */
#define CFG_ENABLE_EVASION   @@ENABLE_EVASION@@
#define CFG_SLEEP_MASKING    @@SLEEP_MASKING@@

/* Malleable C2 profile name */
#define CFG_C2_PROFILE       "@@C2_PROFILE@@"

/* Domain fronting — empty = disabled */
#define CFG_FRONT_DOMAIN     "@@FRONT_DOMAIN@@"

/* Fallback C2 URLs — comma-separated, empty = no failover */
#define CFG_FALLBACK_URLS    "@@FALLBACK_URLS@@"

/* Default execution method: "cmd" | "powershell" */
#define CFG_EXEC_METHOD      "@@EXEC_METHOD@@"

/* Debug mode — "1" emits debug output and skips pre-beacon delay */
#define CFG_DEBUG            @@DEBUG@@

/* TLS certificate pin — lowercase SHA-256 hex of DER-encoded server cert.
 * Empty string disables pinning (default for lab use). */
#define CFG_CERT_PIN         "@@CERT_PIN@@"
